import { BrowserRouter, Routes, Route } from "react-router-dom";

const AppRoute = () => {
  return (
    <BrowserRouter>
      <Routes></Routes>
    </BrowserRouter>
  );
};

export default AppRoute;
